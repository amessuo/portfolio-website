---
import '../styles/globals.css'
import BlogNavbar from '../components/layout/BlogNavbar.jsx'
import BlogFooter from '../components/layout/BlogFooter.astro'

interface Props {
  title?: string
  description?: string
  ogTitle?: string
  ogDescription?: string
  ogType?: string
  publishedTime?: string
  author?: string
}

const {
  title = 'Blog | ThreeDigital',
  description = 'Insights on SEO, AI-powered marketing, and organic growth strategies for B2B businesses.',
  ogTitle,
  ogDescription,
  ogType = 'website',
  publishedTime,
  author,
} = Astro.props
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={ogTitle || title} />
    <meta property="og:description" content={ogDescription || description} />
    <meta property="og:type" content={ogType} />
    {publishedTime && <meta property="article:published_time" content={publishedTime} />}
    {author && <meta property="article:author" content={author} />}
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:wght@600;700&family=Karla:wght@400;500&display=swap"
      rel="stylesheet"
    />
    <link rel="icon" href="/favicon.png" />
  </head>
  <body>
    <BlogNavbar client:load />
    <slot />
    <BlogFooter />
    <script src="../components/scripts/reveal-on-scroll.js"></script>
  </body>
</html>
